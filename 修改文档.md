# SAM3 多尺度与微调改造说明

## 1. 本次改造目标

针对你提出的三个问题，本次在代码端完整落实了以下目标：

1. 让 `SAM3` 在 `FREEZE=False` 时真正可微调，而不是“名义可训、实际无梯度”。
2. 支持从 `SAM3` 原生 `backbone_fpn` 获取多尺度特征，而非只取单层 `[-1]`。
3. 在骨干内部提供可配置的多尺度融合（FPN拼接）策略：`last` / `sum` / `concat`。
4. 提供配置项与最小配置示例，便于直接用于下一轮训练。

---

## 2. 已完成的代码改动

### 2.1 `modeling/backbone/sam3_backbone.py`

已落实以下关键改动：

- **新增配置读取**
  - `MODEL.SAM3.USE_BACKBONE_FPN`
  - `MODEL.SAM3.MULTISCALE_MERGE`（`last | sum | concat`）

- **微调开关生效**
  - `SAM3` 主干模型在 `FREEZE=False` 时切为 `train()`。
  - 主干前向从固定 `torch.no_grad()` 改为：
    - `torch.set_grad_enabled(not self.freeze)`。
  - 1x1 投影层及多尺度逐层投影层在 `FREEZE=False` 时可训练（`requires_grad=True`）。

- **多尺度提取能力**
  - 新增 `_extract_native_multiscale()`：
    - 从 `backbone_out["backbone_fpn"]` 提取所有层级。
    - 自动对每层执行 4D 规范化（`[B,C,H,W]`）与通道投影到 `FEATURE_DIM`。
    - 动态估计每层 stride，返回按 stride 从小到大排序的特征列表。

- **FPN 融合/拼接能力**
  - 新增 `_build_nested_from_multiscale()`：
    - `last`：保留多层，按层输出（不融合）。
    - `sum`：先对齐空间尺寸，再逐层相加融合。
    - `concat`：先对齐空间尺寸，通道拼接，再通过 `1x1` 融合回 `FEATURE_DIM`。
  - 合并后的 mask 采用 OR 规则，保证 padding 区域安全。

- **统一行为**
  - 在线提取路径和预计算特征路径都复用同一套多尺度融合逻辑，行为一致。

---

### 2.2 `configs/defaults.py`

新增默认配置：

- `MODEL.SAM3.USE_BACKBONE_FPN = True`
- `MODEL.SAM3.MULTISCALE_MERGE = "last"`

这样不改老配置也能兼容，且支持新能力。

---

### 2.3 `configs/speaq_actiongenome_minimal.yaml`

已按“可微调 + 多尺度 + 不过粗糙”的策略更新：

- `FREEZE: False`
- `TARGET_STRIDE: 16`
- `USE_FPN: True`
- `FPN_STRIDES: [8, 16, 32]`
- `USE_BACKBONE_FPN: True`
- `MULTISCALE_MERGE: "sum"`
- `SOLVER.BACKBONE_MULTIPLIER: 0.05`

---

## 3. 如何获得 SAM3 的多尺度特征

当前实现的顺序为：

1. 输入图像按 SAM3 规范做 resize/normalize。
2. 调用 `sam3_model.backbone.forward_image(image_batch)`。
3. 若启用 `USE_FPN=True` 且 `USE_BACKBONE_FPN=True`：
   - 从 `backbone_out["backbone_fpn"]` 读取多层特征。
   - 每层转换为 `[B,C,H,W]`。
   - 每层用独立 `1x1 conv` 投影到 `FEATURE_DIM`（或 `Identity`）。
   - 根据 `input_h / feat_h` 动态估计 stride。
4. 将多层特征交给融合器（`last/sum/concat`）生成输出 `NestedTensor`。
5. 若原生多层不可用，回退到单层路径（兼容旧逻辑）。

---

## 4. 如何进行 FPN 拼接（融合）

### 4.1 `last`（不融合）

- 保留全部层级，逐层输出。
- 这适合你想观察每层效果或后续在 Transformer 里做多层消费。

### 4.2 `sum`（推荐默认）

- 先把所有尺度插值到统一空间分辨率（当前实现用最后一层的空间尺寸）。
- 逐层相加得到融合特征。
- 优点：显存和参数开销小，训练稳定。

### 4.3 `concat`（表达更强）

- 先对齐空间分辨率。
- 通道维拼接后经 `1x1` 投影回 `FEATURE_DIM`。
- 优点：保留跨层差异更充分。
- 代价：显存/计算更高，对学习率更敏感。

---

## 5. SAM3(Transformer) 的 FPN 与原始 ResNet101 多尺度有何不同

## 5.1 特征来源机制不同

- **ResNet101**
  - 多尺度来自 CNN 分层 stage（`res2~res5`），层级与感受野关系直观稳定。
- **SAM3**
  - 多尺度来自 Transformer + neck 的 `backbone_fpn` 输出，语义结构不是传统 CNN stage 的一一对应。

## 5.2 “真多尺度”与“伪多尺度”的区别

- **ResNet101**
  - 天然多层输出，通常直接作为多尺度输入。
- **SAM3**
  - 若只拿单层再上/下采样，本质是伪多尺度。
  - 本次改造优先使用 `backbone_fpn` 原生多层，避免伪多尺度问题。

## 5.3 对齐方式差异

- **ResNet101**
  - 常见做法是卷积式 FPN 侧连融合。
- **SAM3**
  - 更依赖通道投影 + 尺度对齐 + token/位置编码一致性。
  - 因此实现中加入了多尺度投影、融合策略和 mask 合并逻辑。

## 5.4 训练行为差异

- **ResNet101**
  - 局部先验强，微调鲁棒性通常较好。
- **SAM3**
  - 全局建模强但更敏感，微调时更依赖：
    - 冻结策略是否真正生效
    - backbone 学习率倍率
    - 输出 stride 是否过粗

---

## 6. 推荐训练策略

1. 先用 `MULTISCALE_MERGE: "sum"` 跑稳定版本。
2. 若指标仍遇瓶颈，再试 `concat` 提升上限。
3. 对于 `FREEZE=False`，建议继续保持较小 `BACKBONE_MULTIPLIER`（如 0.05）。
4. `TARGET_STRIDE` 优先 16，避免关系建模过于粗糙。

---

## 7. 本次修改清单（文件）

- `modeling/backbone/sam3_backbone.py`
- `configs/defaults.py`
- `configs/speaq_actiongenome_minimal.yaml`
- `修改文档.md`（本文档）

---

## 8. 备注

按你的要求，本次仅完成代码与文档落地，未执行任何训练/调试/运行命令，以避免影响当前正在进行的训练任务。

