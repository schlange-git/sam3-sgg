整个流程你都可以（并且鼓励）联网获取资料和协助实现。需求文件原本是写给cursor的，现在由你来实现（也就是文中的cursor现代代指你）
1. /home/cfs/shizekun1_v/sam3-sgg-Auxiliary-Matching/abschluss-paper/sam3_temporal.md是sam3中的时序方案，你需要进行学习
2. 我们的代码库进行一次git add . 以及commit。然后从这个最新的commit新建一个branch：temporal_v3
3. abschluss-paper/triplet_memory_feasibility2.md是一个我们在speaq的sgg任务中对于sam3类似的时序实现的设想。你同样进行学习。并且进行落地
4. 网上资料鼓励自行查阅，但是只允许访问已授权网站，避免权限卡死，如下表
    │           站点            │ curl (Bash) │      用途      │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ arxiv.org                 │ ✅ 200      │ 论文摘要       │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ ar5iv.labs.arxiv.org      │ ✅ 200      │ 论文 HTML 全文 │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ github.com                │ ✅ 200      │ 代码仓库       │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ raw.githubusercontent.com │ ✅ 200      │ 原始文件       │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ paperswithcode.com        │ ✅ 302      │ 方法排行       │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ openaccess.thecvf.com     │ ✅          │ CVF 论文       │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ docs.python.org           │ ✅ 200      │ Python 文档    │
    ├───────────────────────────┼─────────────┼────────────────┤
    │ pytorch.org               │ ✅ 301      │ PyTorch 文档   │
5. 所有的试运行，debug都由你来运行而不是我，每一个阶段代码运行成功的标志都是看到loss，稳定输出50以上iter。
6. 看到eval不代表运行成功，反而更可能是失败了。因为train报错会直接跳过train开始eval。
7. 因为6中的原因，所以我们的启动脚本不要使用eval，有yaml中的间隔iter中间评测足够。
8. 我们这部分的代码实现逻辑，不允许回退。我的意思是这个时序必须落地，不得在任何部分加入，如果失败后的冗余方案或者下位方案，或者零填充等等兼容方案。同时在核心部分加入生效assert。如果时序相关的配置没有生效，直接报错退出，并且进行调试改正。
9. 推荐使用新建yaml以及sh启动文件进行管理，并且及时删除用于调试的sh yaml文件或修复完成的相关debug代码。
10. 在本机器进行单卡运行
11. 本次核心任务是落地3中的v3时序，并且成功运行。git管理推荐最中保留两个，对应两个stage。比如运行完成第一个stage后。如果你落地完stage1后运行有bug，可以尝试debug，解决。每解决一个bug进行amend noedit，进行commit。stage2同理。最终git仓库只有这两个新的commit。
12. 你可以在某些部分有自己的想法，特别是你对于我们已有的代码比出这个方案的gpt5.5理解深刻。但是他的宏观架构是很好的。另外我们并不是不允许修改v2时序内容。你可以尽可能利用已有的内容改造。这也是我让你新建branch的原因。
13. 你需要维护一个md文档。把你比较核心的修改流程，使用文档末尾增加的方式，写记录下来。同时现阶段我们以跑通为目的。stage1跑通即可开始落地stage2.性能方面不需要考虑，也不需要在训练中真的运行处结果，只要有代表成功的loss输出即可。同时为了调试更快。推荐使用之前使用过的1000帧过拟合。
12. 因为你的记忆长度有限，所以每次遇到问题或者每15分钟访问一次该文件（/home/cfs/shizekun1_v/sam3-sgg-Auxiliary-Matching/abschluss-paper/task0606.md）以防止你不会忘记其中的重要内容导致卡死。