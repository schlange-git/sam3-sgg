MODEL:
  META_ARCHITECTURE: "IterativeRelationDetr"
  WEIGHTS: "detectron2://ImageNetPretrained/MSRA/R-101.pkl"
  PIXEL_MEAN: [123.675, 116.280, 103.530]
  PIXEL_STD: [58.395, 57.120, 57.375]
  MASK_ON: False
  RESNETS:
    DEPTH: 101
    STRIDE_IN_1X1: False
    OUT_FEATURES: ["res2", "res3", "res4", "res5"]
  DETR:
    NAME: "IterativeRelationDETR"
    TRANSFORMER: "IterativeRelationTransformer"
    CRITERION: "IterativeRelationCriterion"
    MATCHER: "SpeaQHungarianMatcher"
    NUM_OBJECT_QUERIES: 300
    NUM_RELATION_QUERIES: 300
    # Minimal starter values; override after first annotation statistics check.
    NUM_CLASSES: 50
    NUM_RELATION_CLASSES: 30
    OBJECT_DEC_LAYERS: 6
    REWEIGHT_RELATIONS: True
    REWEIGHT_REL_EOS_COEF: 0.1
    NO_OBJECT_WEIGHT: 0.1
    NO_REL_WEIGHT: 0.1
  SAM3:
    ENABLED: False
    USE_PRECOMPUTED: False
    # SAM3权重路径（如果为空或文件不存在，会尝试从HuggingFace下载）
    CHECKPOINT_PATH: "sam3.pt"
    # SAM3输入图像尺寸
    IMAGE_SIZE: 1008
    # SAM3输出特征维度（需要与DETR的HIDDEN_DIM匹配）
    FEATURE_DIM: 256
    # 通道重复倍数（1表示不重复）
    CHANNEL_REPEAT: 1
    # 是否冻结SAM3参数（True时不参与反向传播，节省显存）
    FREEZE: True
    # SAM3运行的设备（通常与MODEL.DEVICE相同）
    DEVICE: "cuda"
    # 如果使用预计算的特征图，设置特征图目录
    FEATUREMAP_DIR: "data/featuremaps"

DATASETS:
  TYPE: "ACTION GENOME"
  TRAIN: ("AG_train",)
  TEST: ("AG_val",)
  ACTION_GENOME:
    ANNOTATIONS: "/home/shi/桌面/abschluss/sgg/dataset/actiongenome/annotations"
    FRAMES: "/home/shi/桌面/abschluss/sgg/dataset/actiongenome/frames"
    VIDEOS: "/home/shi/桌面/abschluss/sgg/dataset/actiongenome/videos"
    FILTER_EMPTY_RELATIONS: True
    FILTER_DUPLICATE_RELATIONS: True
    FILTER_NON_OVERLAP: False
    FORMAT_VID_WISE: True
    NUM_VIDEOS_TRAIN: -1
    NUM_VIDEOS_VAL: 400
    VAL_SET_RANDOMIZED: False

SOLVER:
  IMS_PER_BATCH: 8
  BASE_LR: 0.0001
  STEPS: (80000,120000)
  MAX_ITER: 150000
  WARMUP_FACTOR: 0.001
  WARMUP_ITERS: 2000
  WEIGHT_DECAY: 0.0001
  OPTIMIZER: "ADAMW"
  BACKBONE_MULTIPLIER: 0.1
  CHECKPOINT_PERIOD: 1000
  CLIP_GRADIENTS:
    ENABLED: True
    CLIP_TYPE: "full_model"
    CLIP_VALUE: 0.1
    NORM_TYPE: 2.0
  AMP:
    ENABLED: True

INPUT:
  MIN_SIZE_TRAIN: (480, 512, 544, 576, 608, 640, 672, 704)
  CROP:
    ENABLED: True
    TYPE: "absolute_range"
    SIZE: (360, 640)
  FORMAT: "RGB"

TEST:
  EVAL_PERIOD: 15000

DATALOADER:
  FILTER_EMPTY_ANNOTATIONS: False
  NUM_WORKERS: 4

VERSION: 2
